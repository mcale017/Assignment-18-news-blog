<div class="container-fluid">
    <div class="row">
        {{#each articleObject}}
            <div class="col-lg-6">
                <div class="card" style="width: 50%">
                    <div class="card-body">
                        <a href="https://www.nhl.com{{link}}" class="btn btn-primary">{{title}}</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>